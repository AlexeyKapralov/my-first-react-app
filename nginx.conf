server {
    listen 3000;
    listen [::]:3000;

    root /usr/share/nginx/html;
    index index.html;

    location / {
       root /usr/share/nginx/html;
    }

    location /static/ {
        root /usr/share/nginx/html;
    }
}
